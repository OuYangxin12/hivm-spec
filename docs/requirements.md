# AI 辅助 pass 开发的快速语义验证能力 — 需求文档

> **状态**：需求基线 v0.2（架构审查后回写：§6 未决问题按处置状态重列、NFR1/NFR5 关闭、§5 优先级确认）。
> **范围声明**：本文档只包含需求、需求分解与未决问题；**不含实现方案、技术选型与架构设计**。此前调研与讨论中出现的任何机制性描述（执行、建模、工具形态等）均属候选方案，一律不在本文档中固化，留待方案讨论阶段决定。
> **输入**：HIVM 可行性调研、preload 特性开发实践经验、多轮需求收敛讨论。

---

## 1. 背景与问题陈述

### 1.1 工程背景

本项目在 pass 开发中采用 vibecoding 实践：AI 代理承担 pass 的编写、调整与迭代。开发主体（AI）在每次修改后需要快速判断"改动是否正确"，并在失败时依据反馈纠正方向。**验证回路的质量直接决定 AI 开发的方向正确性与收敛速度。**

### 1.2 现有验证手段与缺口

现有手段：编译构建 + lit/FileCheck 结构回归（check-mlir / check-bishengir，双架构）+ 真机 E2E（依赖硬件与 CANN 环境）。

| 编号 | 缺口 | 说明 |
|---|---|---|
| G1 | **语义缺位** | FileCheck 类验证是结构性的（IR 形态匹配），不验证计算语义；且 AI 可通过更新期望值使错误输出"合法化"——现有验证可被被验证者操纵 |
| G2 | **反馈速度** | 真机 E2E 资源稀缺、周期长，无法进入开发回路；全量 UT 为分钟级 |
| G3 | **维度缺失** | 三类高频验证目标现有手段完全无法覆盖：计算语义等效（尤其结构全新的 IR）、死锁、片上内存溢出 |

### 1.3 实证案例：preload 特性开发

preload / CV 流水类特性的开发经验实证了 G3：

1. 特性产出**结构全新的 IR**（新循环结构、新 buffer、新同步 op），不存在结构相似的可比对象，任何基于 IR 形态比对的手段均不适用；
2. 需要验证：变换后的计算语义与变换前**等效**；
3. 需要验证：多缓冲与 mmadL1 事件对（`sync_related_args` 中多对 Wait/Set）在**跨迭代时序下不死锁**；
4. 需要验证：变换后 IR 的 UB/CBUF 占用**不溢出**。

---

## 2. 目标与非目标

### 2.1 目标

1. 为 AI 的 pass 开发回路提供**快速、可依赖、防自欺**的语义验证能力；
2. 验证维度覆盖：计算语义等效、死锁、片上内存溢出，并可按需求扩展其他性质；
3. 验证能力可沿开发任务**按需生长**：支持新特性时的扩展成本与该特性引入的新语义面成比例，不为未到来的需求预付全部成本。

### 2.2 非目标

1. 周期精确或性能精确的硬件仿真；
2. 替代现有 lit/FileCheck 结构回归或真机 E2E 验收（三者职责互补，边界见 Q7）；
3. 对任意 IR 给出完备的形式化证明；
4. 初版即覆盖 RegBase/DPX 路径（见 Q5）。

---

## 3. 用户与使用场景

| 用户 | 说明 |
|---|---|
| U1 AI 代理（主用户） | 在 pass 开发回路中程序化触发验证、消费结构化结论 |
| U2 人类开发者 | 评审 AI 工作时抽查、复现问题 |
| U3 CI 门禁 | PR 级验证。**已落地**（D5）：lint+types / core 测试矩阵 / schema golden / spec-gate 四门禁随 PR 强制 |

| 场景 | 描述 |
|---|---|
| S1 | 修改既有 pass：验证改动未破坏计算语义 |
| S2 | 新特性开发（新 IR 形态）：验证语义等效 + 死锁 + 溢出（preload 实证场景） |
| S3 | 验证失败：诊断信息足以支撑 AI 在下一轮修正方向 |
| S4 | 真机发现问题的无硬件复现（仍未决，见 Q10；收敛时点 M3 后） |

---

## 4. 需求分解

### 4.1 功能需求

| 编号 | 需求 | 说明 |
|---|---|---|
| FR1 | **语义等效验证** | 判定两份**允许结构完全不同**的 IR 计算语义是否等效；判定机制不得依赖 IR 结构相似性（锚点选择属方案层，见 Q1） |
| FR2 | **死锁检测** | 判定 IR 在 HIVM 同步语义（pipe/event/sync_block）下是否存在无法推进的执行；对"未发现死锁"的结论须附带其判定边界口径（见 Q2） |
| FR3 | **片上内存溢出检测** | 判定 IR 对地址空间（UB/CBUF 等）的占用是否可能超过硬件限制 |
| FR4 | **按需的覆盖生长** | 遇到验证能力未覆盖的 op/特性时，必须**显式、明确地报告缺口**（禁止静默跳过或给出不可信结论）；补齐缺口的成本与任务引入的新语义面成比例 |
| FR5 | **可诊断性** | 验证失败时输出可行动的诊断：定位到 op/源位置/触发用例，并说明问题类别（语义/死锁/溢出），使 AI 能据此修正开发方向 |
| FR6 | **防自欺性** | 验证结论不可通过修改期望值、参考数据或断言而改变；判定依据必须独立于被验证者可控的工件。**注**：描述库本身是被验证者可改的工件，故其变更须受机器门禁约束（D5 spec-gate：描述变更强制对拍、trust 升级强制附证据），而非仅靠评审自律 |
| FR7 | **元可信性（区分谁出错）** | 能区分"被测 IR 有问题"与"验证机制自身不可用/出错"两类失败；对后者的处理方式不得误导开发方向 |
| FR8 | **确定性** | 相同输入与相同被测 IR，验证结论可复现 |
| FR9 | **程序化可用** | AI 可在开发回路中程序化触发验证并以结构化形式读取结论（接口形态见 Q6）。**注**：工具为单 IR 输入的原子操作，多步定位（如跨 pass bisect）由 AI 自行编排（D12） |
| FR10 | **可扩展的性质清单（候选）** | 未初始化片上内存读取、同步配对完整性、layout 合法性等候选验证维度。**取舍已定**（Q8 关闭）：M4 纳入未初始化读（T4.3）+ 同步静态配对（T4.4）；layout 合法性暂不纳入 |

### 4.2 非功能需求

| 编号 | 需求 | 说明 |
|---|---|---|
| NFR1 | **速度** | 开发回路内的反馈时间预算，**已确认为基线**（D10，架构审查关闭原"建议值待确认"）：UB 占用图 ≤ 10s/kernel；时序图 ≤ 30s/kernel；等价验证（具体档）≤ 5min/kernel 对；特性级完整验证 ≤ 10min。各里程碑退出时实测回填，超限触发 D1 复查条款 |
| NFR2 | **正确性基线** | "基本可靠"的量化含义：**误判（把正确的改动判错）**是最高优先级压制目标且必须可甄别；漏报可接受但须可度量（见 Q3） |
| NFR3 | **无硬件依赖** | 验证回路的运行不得依赖真机或 CANN 运行时 |
| NFR4 | **增量成本** | 见 FR4；本条强调其作为长期约束：验证能力不应随方言演进而需要一次性全量重建 |
| NFR5 | **语义同步** | 验证所依据的 HIVM 语义与编译器/硬件语义演进保持一致的机制。**机制已定**（D9 + design-framework §8.1）：漂移登记 drift ledger + 冻结 trust 升级 + 主仓 C++ 链默认权威 + 硬件 golden 仲裁；账本绊线检测方言新增 op |

### 4.3 约束

| 编号 | 约束 |
|---|---|
| C1 | HIVM 为封闭 ISA（**实测 114 个已注册 op**，与 scf/arith/memref/tensor 等社区方言深度混用）——验证覆盖空间有限且已知 |
| C2 | 动态 shape 与运行时 tiling 参数被广泛使用（约六成 HIVM 测试含动态维度）——验证须适用于运行时形状 |
| C3 | A3（membase）与 A5（regbase）双路径在 master 并存。**不分叉**（Q5 关闭 / OD7）：op 语义一份共享，`arch` 段只存差异（容量常量 + regbase 特有 op 分文件），`Spec(arch=...)` 参数化 |

### 4.4 验收标准（需求级）

| 编号 | 标准 |
|---|---|
| AC1 | 以 preload 类特性为案例：对**注入已知缺陷**的 IR（语义错误 / 死锁 / 溢出各一），验证能力能分别检出并给出符合 FR5 的诊断 |
| AC2 | 反馈时间满足 NFR1 预算 |
| AC3 | AI 依据失败诊断在下一轮修正方向的比率可度量且达到约定阈值（度量方法见 Q4，绑定 M3 末收敛——等价验证可用后才有失败诊断样本可统计） |
| AC4 | 防自欺测试：篡改期望值/参考数据后，验证结论不发生改变 |

---

## 5. 优先级

| 级别 | 内容 | 理由 |
|---|---|---|
| P0 | FR1、FR5、FR6、FR8、FR9、NFR1、NFR3 | 构成最小可用回路：快速、防自欺的语义验证 |
| P1 | FR2、FR3、FR7 | 特性开发（preload 类）的一等验证目标与可信度保障 |
| P2 | FR4 深化、FR10、S4 | 覆盖扩展（U3/CI 集成已于 D5 落地，不再列为 P2） |

> **优先级已确认**（架构审查关闭原"待讨论"）。
>
> **须注意的排期张力**：P0 的 FR1（语义等效）在方案层排在 M3/M4——即最后交付，
> 而 P1 的 FR2/FR3 反而在 M1/M2 先行。该顺序有其理由（D3：状态视图工程最薄，
> 且 `vm` 状态模型是等价工具的前置地基，先建不返工），但代价是 P0 兑现最晚。
> 对冲机制见 **D8**：M3 的最小等价内核（差分执行 + 值哈希 trace + 首发散点定位）
> 被定义为**不可裁剪交付项**，范围可缩、能力不可删；M1 须为解释执行预留 VIR 接口。
> **若 M2 后停工，本表的 P0 即未兑现**——这是排期上必须持续意识到的风险。

---

## 6. 未决问题的处置状态

> **状态同步（架构审查修正）**：本节原为"下一步讨论议程"，其中多数问题已在方案层
> （`design-framework.md` D1–D12 / `industry-research.md` OD1–OD12）关闭，但未回写至本文档，
> 导致"哪些决策已封闭"本身不可信。现按处置状态重列；**方案层的决策细节不在本文档展开**，
> 只登记落点，以保持本文档"只含需求"的范围声明。

### 6.1 已关闭（决策落点见右列）

| 编号 | 问题 | 处置与落点 |
|---|---|---|
| Q1 | 语义等效的锚点机制 | **OD6**：三级锚点分层——默认差分锚点=变换前 IR；第二锚=上层源 IR（HFusion）；第三=稀疏硬件 golden。符号等价限值语义子集 |
| Q2 | 死锁检测的保真度边界与报告口径 | **OD2**：两层——wait-for 图确定性层（结构性死锁，确定性结论）+ 有界策略探索层，后者统一措辞"在{策略集}×{展开界}内未发现" |
| Q5 | A3/A5 双架构是否分叉 | **OD7**：单模型 + `arch` 差异段参数化（容量常量 + regbase 特有 op 分文件）；禁止两份全量描述 |
| Q6 | 使用接口形态与 FR9 具体化 | **D2 + FR9**：薄 CLI（`hivm-spec <cmd>`）+ JSON 结构化结论契约（框架 §7.3）；未实现阶段以退出码 3 报 PENDING |
| Q8 | FR10 候选性质清单取舍 | **M4 范围已定**：未初始化片上内存读取（T4.3）+ 同步静态配对完整性（T4.4）；layout 合法性暂不纳入 |
| Q9 | 是否/何时进入 CI 门禁（U3） | **D5**：已进入。CI 四门禁（lint+types / core 矩阵 / schema golden / spec-gate）随 PR 强制；描述变更触发对拍集，trust 升级强制附证据 |
| Q11 | NFR5 语义演进同步机制 | **D9 + 框架 §8.1**：漂移登记 + trust 冻结 + 主仓 C++ 链默认权威 + 硬件 golden 仲裁；账本绊线检测方言新增 op |
| Q12 | 浮点容差与舍入语义判定标准 | **OD3**：rtol+atol（全局默认 + per-op 覆盖，按 dtype 缩放）；`round_mode` 在值语义中显式表达；f16/bf16 用 ml_dtypes |

### 6.2 仍未决（需求层，须在对应里程碑前收敛）

| 编号 | 问题 | 收敛时点 |
|---|---|---|
| Q3 | 覆盖与漏报的**度量口径**：按 op？按特性场景？如何持续度量 | M1 末（账本已能输出 modeled/unmodeled 后，据实际数据定口径） |
| Q4 | AC3 中"AI 纠正方向的比率"如何度量 | M3 末（等价验证可用后才有失败诊断样本可统计） |
| Q7 | 与现有 lit/FileCheck 结构回归、真机 E2E 的**职责边界** | M1 末（评估 lit 接入 `check-bishengir` 时一并明确） |
| Q10 | S4（真机问题的无硬件复现）是否纳入首期 | M3 后评估（依赖等价验证与硬件 golden 锚定流程成熟度） |

> Q3/Q4 是**项目价值的最终度量口径**，长期悬空会使 AC1/AC3 无法验收；已分别绑定到
> M1/M3 退出检查项，不再无限期挂起。

---

## 7. 术语表

| 术语 | 定义 |
|---|---|
| 快速失败 | 验证在开发回路时间预算内给出明确结论，失败时提供可行动的定位信息 |
| 防自欺性 | 验证结论不能被被验证者（尤其 AI）通过修改期望、参考或断言而改变 |
| 语义等效 | 两份结构不同的 IR 在约定精度口径（Q12）下计算结果一致 |
| 元可信性 | 验证机制能区分"被测对象有问题"与"验证机制自身不可用" |
| 按需生长 | 验证覆盖沿开发任务引入的新语义面增量扩展，扩展成本与任务规模成比例 |
| 误判 / 漏报 | 把正确的改动判为失败 / 把有问题的改动判为通过 |
